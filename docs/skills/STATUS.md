# Skill bridge status

| Item | State |
|---|---|
| skillDir default | `$DSH_HOME/dsh-image-workstation/skills` |
| five repos cloned | yes |
| discover → 6 UI entries | yes（封面派生） |
| host `listSkills` / `planSkill` / stitch / poster | provide 已挂 |
| Skill RPC | `/dsh-ws-skill` `list` \| `plan`（**不**经 mediaProxy） |
| UI 想方案 | 选 skill → plan → 方案卡 → 就这样出图填 prompt |
| score gates CTA | **never** (`disabledByScore: false`) |
| CTA / mediaProxy | **untouched** by skill wiring |
