# Skill bridge status

| Item | State |
|---|---|
| skillDir default | `$DSH_HOME/dsh-image-workstation/skills` |
| five repos cloned | yes |
| discover → 6 UI entries | yes（封面派生） |
| host `listSkills` / `planSkill` / stitch / poster scripts | provide 已挂 |
| score gates CTA | **never** (`disabledByScore: false`) |
| CTA / mediaProxy | **untouched** by skill work |
| UI「想方案」接线 | paused until cores 真出图 |
