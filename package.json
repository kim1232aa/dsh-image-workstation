{
  "name": "dsh-image-workstation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "dsh 生图工作台 — Cordis plugin (rebuild, not a VisioWork/Nova fork)",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./client": "./lib/client.js"
  },
  "files": [
    "src",
    "lib",
    "cordis.patch.yml",
    "README.md",
    "VERSIONS.txt",
    "docs"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "inject": [
        "@deepseek-ai/dsh-api-remotes",
        "@deepseek-ai/dsh-api-session-controller",
        "@deepseek-ai/dsh-client-connection",
        "@deepseek-ai/dsh-client-locale",
        "@deepseek-ai/dsh-client-ui-conversation",
        "@deepseek-ai/dsh-client-ui-renderer",
        "@deepseek-ai/dsh-client-ui-settings",
        "@deepseek-ai/dsh-client-ui-sidebar",
        "@deepseek-ai/dsh-client-ui-tool"
      ],
      "platform": "web"
    }
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.2"
  },
  "scripts": {
    "check": "node --check src/index.js && node --check src/config.js && node --check lib/client.js",
    "build:client": "esbuild src/client.js --bundle --format=cjs --platform=browser --outfile=/tmp/ws-client.cjs && node scripts/wrap-client.mjs",
    "verify": "node scripts/verify-redlines.mjs",
    "build": "pnpm run build:client && pnpm run check && pnpm run verify"
  },
  "dependencies": {
    "@deepseek-ai/schemastery": "^3.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
