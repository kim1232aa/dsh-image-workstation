# Host row by package name. Browser half loads via package.json dsh.client
# (served at /plugins/<packageName>/client.js) — same split as VisioWork; this is a rebuild.
# Package name MUST stay ASCII `dsh-image-workstation` (/plugins URL-safe).
# insert id `imagegen-生图工作台`: imagegen + 「生图」 both hit host inventory matches().
# SETTINGS_NAMESPACE stays dsh-image-workstation.
- insert:
    - id: imagegen-生图工作台
      name: dsh-image-workstation
